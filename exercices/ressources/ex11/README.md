# Instructions pour lancer nos services

1. Ouvrir Docker Desktop sur Windows
2. Ouvrir le terminal et se placer dans le dossier racine ou se trouve [docker-compose.yml](./docker-compose.yml)
3. Lancer la commande suivante
```bash
docker compose up -d
```